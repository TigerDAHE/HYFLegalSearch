# AgenticSearch 法律问答系统项目报告

## Background

在中国大陆法语境下，法律问答任务具有三个典型特点：

1. 知识更新频繁：法律、司法解释与地方性规定会持续更新，纯参数化记忆难以保证时效性。
2. 问题形态差异大：从“单一法条核实”到“多要件案情推理”，复杂度跨度大。
3. 可追溯性要求高：实际使用中，回答需要来源依据、摘要解释和风险提示，不能仅输出结论。

基于上述背景，本项目构建了一个面向中文法律问答的 AgenticSearch MVP：通过“先检索后路由”的工作流，把检索、分流、推理和生成拆分为可观测的节点，降低大模型一次性端到端回答的幻觉风险。

## Motivation

本项目的核心动机是把“可用”提升到“可控”。具体包括：

1. 提升回答可靠性：引入外部检索与证据筛选，减少无依据回答。
2. 提升任务适配能力：通过 Router 将不同问题送入不同 pipeline，避免“一套策略打天下”。
3. 提升工程可运维性：在后端与前端均加入结构化日志，支持请求级追踪与故障排查。
4. 提升系统韧性：针对网络检索链路增加重试、退避和降级机制，避免单点异常导致全流程失败。

## Technique

### 1) 系统总体架构

系统采用 FastAPI + LangGraph + LiteLLM + Serper 的模块化架构：

- API 入口与编排：负责请求接入、工作流调用、响应组装。
- 工作流图：负责节点化执行、条件分流与 loop 控制。
- 模型路由：统一接入多模型提供方（可切换模型）。
- 联网检索：固定使用 Serper，结合网页抓取与证据筛选。
- 前端界面：最小可用问答界面，支持模型输入与链路日志。

### 2) 工作流关键技术步骤

当前实现的主链路如下：

用户提问 -> 法律实体/关键词提取 -> 首轮联网搜索(1次) -> 搜索摘要 -> Router 分流 -> 进入 pipeline -> 生成答案

对应关键节点：

1. `extract_legal_entities`
- 功能：从用户问题中提取检索关键词与主检索 query。
- 作用：降低后续检索噪声，形成标准化首轮检索入口。

2. `initial_search`
- 功能：执行首轮联网搜索并写入状态。
- 作用：为 router 提供“问题 + 初始外部上下文”。

3. `summarize_initial_search`
- 功能：将首轮结果压缩为结构化摘要（依据线索、时效/地域差异、证据不足点）。
- 作用：提升路由判断质量，避免 router 仅基于原问题猜测。

4. `route_pipeline`
- 功能：输出 pipeline 编号（1/2/3）及理由。
- 作用：将问题分配到差异化处理策略。

### 3) 三类 pipeline 设计

1. Pipeline 1：简单常识核实（搜1次）
- 节点：`pipeline_1_checklist` -> (`pipeline_1_generate` 或 fallback 到 pipeline 2)
- 检查清单：时间效力、地域差异、形式要件、主体资格。
- 特点：如果发现必须精确法条对比，自动回退到 Pipeline 2。

2. Pipeline 2：法条检索对比（搜 n 次）
- 节点：`pipeline_2_decompose` -> `pipeline_2_targeted_search` -> `pipeline_2_synthesize`
- 方法：将问题拆为 1-3 个法律要件，逐项定向检索并逻辑缝合。
- 目标：处理法条竞合、地方差异与条文解释差异。

3. Pipeline 3：案情推理分析（搜 n*m 次）
- 节点：`pipeline_3_decompose` -> `pipeline_3_targeted_search` -> `pipeline_3_decide_loop` -> `pipeline_3_synthesize`
- 方法：拆成 3-5 个要件，按轮次扩展检索词并判断是否继续循环。
- 目标：适配复杂案情中的事实-规范映射与救济路径分析。

### 4) 关键函数与工程机制

1. 检索与聚合函数
- `_first_search_once`：首轮检索。
- `_merge_search_results`：去重聚合多 query 检索结果。
- `_select_useful_sources`：候选来源抓取、LLM 相关性筛选、兜底策略。

2. 生成函数
- `_generate_answer_from_sources`：统一答案生成模板，强制引用标注与风险提示。
- `_build_citations`：生成结构化引用对象（标题、链接、摘要、相关性）。

3. 鲁棒性与可观测性机制
- Serper 网络容错：重试、指数退避、查询清洗、异常降级为空结果。
- 后端日志：请求开始/结束、耗时、pipeline 路由、节点状态。
- 前端日志：提交、响应、渲染、失败分支与请求关联。

## Evaluation

### 1) 评估目标

从工程可用性与答案质量两条线进行评估：

1. 工程可用性
- 服务稳定启动与接口可用率。
- 网络抖动场景下的流程连续性（是否能降级而不中断）。
- 日志可追踪性（是否能定位失败节点与请求链路）。

2. 回答质量
- 路由准确性：问题是否被分配到合理 pipeline。
- 证据质量：引用是否相关、可访问、可解释。
- 回答完整性：是否包含结论、依据、不确定性说明、风险提示。

### 2) 当前结果（基于项目现状）

1. 已完成项
- 三路 pipeline 已实现并可执行。
- 前后端结构化日志已接入。
- Serper 连接异常（如 WinError 10054）已有重试与降级策略。

2. 已观察到的现象
- 在代理/TLS不稳定环境下，联网检索可能出现连接中断。
- 容错策略可将失败限制在单次检索，不会直接导致整个问答流程崩溃。

3. 仍待补充的系统化评测
- 尚未形成固定数据集上的量化 benchmark（如路由准确率、引用准确率、端到端成功率）。
- 尚未建立人工标注集用于法律正确性与可采纳性评估。

### 3) 建议的可复现实验方案

1. 数据集构建
- 按三类任务各采样若干问题（简单核实/法条对比/案情推理）。
- 每题提供人工参考要点与参考法条链接。

2. 指标设计
- 路由准确率：router 预测类别与人工标签一致率。
- 引用命中率：引用是否支持结论、是否可访问。
- 回答结构完整率：是否满足“结论-依据-不确定性-风险提示”模板。
- E2E 成功率：在含网络抖动条件下成功返回可用答案的比例。

3. 消融实验
- 去掉首轮摘要后比较 router 准确率。
- 去掉 pipeline 分流后比较复杂问题回答质量。
- 关闭重试机制后比较网络异常下 E2E 成功率。

## Discussion & Future Work

### 1) 当前局限

1. 法律知识权威性依赖外部网页来源，来源质量不稳定。
2. 检索与筛选高度依赖 LLM 提示词，存在任务漂移风险。
3. Pipeline 3 的 agentic loop 轮数较保守，复杂案件可能仍检索不足。
4. 目前缺少离线评测闭环与自动回归测试集。

### 2) 未来工作方向

1. 来源治理
- 增加权威站点白名单/黑名单与来源可信度打分。
- 引入法规库/裁判文书结构化数据接口，降低网页噪声。

2. 推理增强
- 在 Pipeline 2/3 中加入“要件-证据-结论”显式中间表示。
- 增加冲突法条判定规则与优先级模板，提高可解释性。

3. 评测体系
- 建立分任务评测集与人工标注标准。
- 接入自动化回归评测，跟踪版本迭代中的质量变化。

4. 工程与体验
- 增强请求级全链路追踪（前端日志、后端日志、节点日志统一 request-id）。
- 增加缓存与异步检索策略，降低响应延迟。
- 在前端展示“路由原因 + 证据摘要 + 不确定性来源”，提升用户可理解性。

---

本报告对应当前 AgenticSearch MVP 实现，重点展示了从“单步问答”向“可分流、可追踪、可降级”的工程化法律问答系统演进路径。
